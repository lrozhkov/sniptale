{
  "schemaVersion": 1,
  "artifactKind": "sniptale-trusted-admission-policy",
  "lanes": {
    "proof": {
      "claim": "fast-pr-gate",
      "fullVitest": true,
      "releaseReady": false,
      "freshPhases": ["install", "verify-project-toolchain", "provision-canvas", "verify-canvas", "provision-ast-grep", "verify-ast-grep", "proof"],
      "derivedPhases": ["non-gate-input-reuse"],
      "requiredFiles": [
        ".tmp/qa/build-proof.json",
        ".tmp/qa/unit-proof.json",
        ".tmp/semgrep/results.json",
        ".tmp/semgrep/results.sarif",
        ".tmp/osv/results.json",
        ".tmp/gitleaks/report.json",
        ".tmp/npm-audit/results.json",
        ".tmp/npm-audit/signatures.json"
      ],
      "requiredPrefixes": [".tmp/qa-observability/runs/", ".tmp/qa-logs/"],
      "reuse": { "build": ["fresh", "reused"], "unit": ["fresh", "reused"], "codeql": ["unavailable"], "coverage": ["unavailable"] }
    },
    "release": {
      "claim": "release-provenance",
      "fullVitest": true,
      "releaseReady": true,
      "freshPhases": ["install", "verify-project-toolchain", "provision-canvas", "verify-canvas", "provision-ast-grep", "verify-ast-grep", "release"],
      "derivedPhases": [],
      "requiredFiles": [
        ".tmp/qa/build-proof.json",
        ".tmp/qa/unit-proof.json",
        ".tmp/qa/codeql-proof.json",
        ".tmp/qa/coverage-proof.json",
        ".tmp/coverage/canonical/coverage-final.json",
        ".tmp/coverage/canonical/coverage-summary.json",
        ".tmp/coverage/canonical/lcov.info",
        ".tmp/semgrep/results.json",
        ".tmp/semgrep/results.sarif",
        ".tmp/codeql/results.filtered.sarif",
        ".tmp/osv/results.json",
        ".tmp/gitleaks/report.json",
        ".tmp/npm-audit/results.json",
        ".tmp/npm-audit/signatures.json",
        ".tmp/licenses/summary.json",
        ".tmp/licenses/sbom.cdx.json"
      ],
      "requiredPrefixes": [
        ".tmp/qa-observability/runs/",
        ".tmp/qa-logs/",
        ".tmp/coverage/canonical/html/",
        ".tmp/mutation/persistence/",
        ".tmp/mutation/secrets/"
      ],
      "reuse": { "build": ["fresh", "reused"], "unit": ["fresh", "reused"], "codeql": ["fresh", "reused"], "coverage": ["fresh", "reused"] }
    }
  },
  "proof": "A pull request executes its candidate QA controls once. Trusted base validates mandatory phases, candidate identity, proof schema, evidence hashes, execution compatibility, artifact closure, reuse disposition, and final graph status without re-executing the previous controls. Both control digests and candidate-controls disposition remain explicit; cross-digest reuse and docs-only derivation are forbidden, and release provenance requires the accepted main control digest.",
  "rollback": "Reject the candidate bundle and execute a fresh gate after restoring the trusted admission owner."
}
