{
  "$comment": "Multi-message orchestration policy data. Include only reviewed owner exceptions and update when messaging seams move.",
  "allowedOwners": [
    {
      "file": "apps/extension/src/composition/frame-annotation-raster-client/orchestrator.ts",
      "function": "runAdmittedRasterTransition",
      "owner": "Frame annotation raster client",
      "justification": "Owns the prepare, staged raster request, cancellation and cleanup transition as one operation.",
      "reviewNote": "Retain while MV3 offscreen raster jobs require an explicit lease and cancellation message."
    }
  ]
}
