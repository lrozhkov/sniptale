import type React from 'react';

export const VIDEO_EDITOR_PANEL_STYLE: React.CSSProperties = {
  background: 'color-mix(in srgb, var(--sniptale-color-surface-panel) 98%, transparent)',
};

export const VIDEO_EDITOR_STAGE_STYLE: React.CSSProperties = {
  background: 'color-mix(in srgb, var(--sniptale-color-surface-panel) 96%, transparent)',
};
