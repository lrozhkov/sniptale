export type ViewerCaptureMode = 'visible' | 'full';

export type ViewerCaptureViewport = {
  height: number;
  width: number;
};

export type ViewerCaptureDocumentMetrics = {
  captureHeight: number;
  captureWidth: number;
  offsetX: number;
  offsetY: number;
};
