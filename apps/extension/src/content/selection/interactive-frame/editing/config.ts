import type {
  InteractiveFrameHandlerConfig,
  InteractiveFrameListenerConfig,
  InteractiveFrameSyncConfig,
} from '../controller/types';

export function createInteractiveFrameSyncConfig(params: InteractiveFrameSyncConfig) {
  return params;
}

export function createInteractiveFrameListenerConfig(params: InteractiveFrameListenerConfig) {
  return params;
}

export function createInteractiveFrameHandlerConfig(params: InteractiveFrameHandlerConfig) {
  return params;
}
