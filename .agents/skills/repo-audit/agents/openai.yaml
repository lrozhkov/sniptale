interface:
  display_name: 'Repo Audit'
  short_description: 'Audit Sniptale architecture, security, tooling, docs, and QA drift'
  default_prompt: 'Use $repo-audit in a new auditor spawned with fork_turns: "none" and supplied an explicit bounded audit manifest, exact repository/diff scope, and QA results; inspect structural/advisory/topology policy drift without running the detector.'
